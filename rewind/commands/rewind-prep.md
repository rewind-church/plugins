---
description: Prep a sermon grounded in your church's own corpus (anti-repetition, coverage, illustrations).
argument-hint: [passage or topic]
---

Use the **Sermon prep** skill to help me prepare a message.

Passage / topic: $ARGUMENTS

Ground everything in our church's own preaching via the `rewind` MCP tools: check
what we've already preached (so I build instead of repeat), check scripture coverage,
pull relevant past illustrations with their attribution intact, and propose a grounded
direction. Cite every corpus claim (sermon, date, speaker, timestamp). If we haven't
preached it, say so plainly.
